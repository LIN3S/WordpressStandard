{% extends 'layout/base.twig' %}

{% block content %}
    {% for post in posts %}
        <div class="grid-block">
            <h1>{{ post.title }}</h1>
            {{ post.content }}
        </div>
    {% endfor %}
{% endblock %}
